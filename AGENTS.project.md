# Boundless Studio

当前工作树是本次修复的交付来源。预览、镜像和 GitHub `main` 只有在用户明确授权部署或 Git 操作时才同步；不要默认推送或覆盖远端。

- 保留已有未提交修改，按任务范围最小化变更；未经明确授权不要执行 commit、push、reset、rebase 或覆盖性清理。
- 不要用旁路分支冒充已部署结果，也不要合并会清理 Key、密码或其它凭据的变更。
- 用户没要过的入口不要挂在顶栏/首页（抽帧路由可留，不当默认产品门）。
- 外部 API 字段、路径和状态必须以对应官方文档或可复现的本地合同测试为依据；无法验证时显式报错，不编造兼容字段。
