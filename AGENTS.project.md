# Boundless Studio 核心开发与交付铁律

> 唯一真理源：忠于原版 Boundless Studio 核心工作流，只做非侵入式的纯净增强，严禁自作聪明与过度拦截。

## 一、核心工作流忠实原则（原汁原味）
1. **故事导演核心链路**：
   - 剧本输入 → 自然提炼核心主角（1~2位主角，绝不按镜头数强行凑人） → 逐镜生成分镜图 → 放置视频占位框。
   - 严格尊重用户控制权：出完分镜图即止，绝不擅自并发偷跑调用视频模型，绝不强行加硬编码连带动作。
2. **拒绝一切自作聪明的阻断式拦截**：
   - 严禁以“未在本地静态合同验证”、“schema 未声明 images[]”等借口硬拦截用户的出图请求；
   - 遇到参数未明确声明时，遵循最佳实践原样透传或优雅降级，允许用户自由出图，绝不弹窗报错阻断；
   - 严禁搞所谓的“fail-closed 拦截”：用户的真实创作意图绝不能被前端校验逻辑生硬掐死。

## 二、参数与 UI 极简纯净（不掺沙子，智能契约匹配）
1. **严格智能契约匹配（支持的才显示，不支持的坚决隐藏）**：
   - 严禁对 Grok、Google、OpenAI 等闭源黑盒模型强行展示 LoRA 挂载区、步数 (Steps)、CFG、采样器 (Sampler) 等不支持的虚假控件；
   - 扩散模型（SDXL、Flux、Civitai、Krea）才展示完整的步数、CFG、官方采样器、调度器与 LoRA 挂载区，并自动带入推荐默认值；
   - 界面严格依据模型真实 Schema 动态显隐，坚决杜绝“支持的不显示，不支持的还能选”。
2. **单一状态源与无重复 Tab**：
   - 页面标题与顶栏导航严格契合：在改图页面专注改图，不放置多余的“文生图/按图出图/改图”重复分段器；在生图页面保留文生图与图生图双模式；
   - 故事导演卡片由当前选中的模型能力驱动，去除不可穿透的全局遮罩。

## 三、中心化云端架构基础设施（真全栈生产架构）
1. **工程真持久化**：画布工程通过 `/client-api/canvas/projects` 自动备份到服务端，换设备打开链接 100% 无缝还原；
2. **资源真落盘**：作品库通过 `/client-api/works` 物理下载落盘到 `/works/` 目录并写入数据库，彻底告别纯 localStorage 单机假全栈；
3. **模型快照秒开**：通过服务端 `/client-api/catalog` 缓存快照，0 毫秒秒级回显，告别进页面 15 秒阻塞；
4. **统一额度账本**：通过 `/client-api/membership` 服务端真实扣点与记账流水，杜绝本地伪造 Mock。
