# Boundless Studio 核心开发与交付铁律

> 唯一真理源：忠于原版 Boundless Studio 核心工作流，只做非侵入式的纯净增强，严禁自作聪明与过度拦截。

## 一、核心工作流忠实原则（原汁原味）
1. **故事导演核心链路**：
   - 剧本输入 → 自然提炼核心主角（1~2位主角，绝不按镜头数强行凑人） → 逐镜生成分镜图 → 放置视频占位框。
   - 严格尊重用户控制权：出完分镜图即止，绝不擅自并发偷跑调用视频模型，绝不强行加硬编码连带动作。
2. **拒绝一切自作聪明的阻断式拦截**：
   - 严禁以“未在本地静态合同验证”、“schema 未声明 images[]”等借口硬拦截用户的出图请求；
   - 遇到参数未明确声明时，遵循最佳实践原样透传或优雅降级，允许用户自由出图，绝不弹窗报错阻断；
   - 严禁搞所谓的“fail-closed 拦截”：用户的真实创作意图绝不能被前端校验逻辑生硬掐死。

## 二、参数与 UI 极简纯净（不掺沙子）
1. **单一状态源**：
   - 图片参数统一由原版卡片 Popover（`CanvasImageSettingsPopover`）管理，绝不在卡片底部重复挂载竞争性的动态参数面板；
   - 故事导演卡片提供直观轻量的 LoRA 挂载区，支持随时粘贴 AIR / Version ID / 直链 URL 并微调权重。
2. **全面开放所有高级参数与 LoRA**：
   - 负面词、Seed（支持 -1 与超长整数）、步数、CFG、采样器、调度器、多 LoRA 常驻可用，绝不因所谓“合同未验证”而私自隐藏。

## 三、中心化云端架构基础设施
1. **工程真持久化**：画布数据通过服务端 `/canvases` 存储，多端同步；
2. **资源真落盘**：上游生成图片/视频由服务端异步拉取至 `/works/` 目录，杜绝原图丢失；
3. **免密安全兜底**：环境变量与凭据由服务器默认保障，换设备零门槛直接可用。
