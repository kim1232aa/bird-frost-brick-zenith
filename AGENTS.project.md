# Boundless Studio

预览工作区 `/workspace` 和 GitHub `main`（`kim1232aa/bird-frost-brick-zenith`）必须是同一份代码。

- 改完立刻：工作区文件 + 推到 `main`。不要只改一边。
- 不要用旁路分支当交付。不要合并会清 Key 的 PR。
- 用户没要过的入口不要挂在顶栏/首页（抽帧路由可留，不当默认产品门）。
